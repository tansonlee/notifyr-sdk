export declare const insertNotification: ({ apiKey, title, body, type, groupId, }: {
    apiKey: string;
    title: string;
    body: string;
    type: string;
    groupId: string;
}) => Promise<any>;
